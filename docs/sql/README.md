# SQL 문서

로컬 SQLite 스키마와 분석 쿼리의 기준을 관리합니다.

| 파일 | 역할 |
|---|---|
| `local_schema.sql` | 쿠폰, 멤버십, 이미지, 알림 설정/로그 테이블 기준 |

## 관리 기준

- 앱의 실제 로컬 DB 구조가 바뀌면 이 스키마도 함께 갱신합니다.
- 테이블과 필드 의미는 `architecture/` 문서와 맞춥니다.
- 분석용 이벤트와 KPI는 `analytics/` 문서를 우선 확인합니다.

